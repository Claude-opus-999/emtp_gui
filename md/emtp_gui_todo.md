# EMTP GUI 待办事项清单

> **生成日期**：2026-05-15
> **基于**：`emtp_gui_dev_plan_v2.md` v2.4 + 代码实际状态审计
> **总体进度**：约 85%（S0/S2/S3/S4/S5 已完成，S1/S6 基本可用，S7 未开始）

---

## 一、按优先级排序的全局清单

| # | 优先级 | 阶段 | 任务 | 当前状态 | 说明 |
|---|--------|------|------|---------|------|
| 1 | **P0** | S0 | 验证清单（5案例端到端运行） | ❌ | S0 最后 5%，阻塞验收 |
| 2 | **P0** | S3 | 验证引擎 UI 集成 + ValidationPanel | ✅ | 面板+工具栏按钮+定位跳转已完成 |
| 3 | **P1** | S1 | SimRunner → QProcess 替代 | ❌ | 大算例稳定性 |
| 4 | **P1** | S1 | 进度解析 + 进度条 UI | ❌ | stdout 提取 step/total |
| 5 | **P1** | S1 | 运行目录管理（独立目录隔离） | ❌ | 每次运行创建独立工作目录 |
| 6 | **P1** | S2 | 光标测量（十字线 + 数值显示） | ✅ | PlotPanel 光标开关+十字线+数值 |
| 7 | **P1** | S2 | 多曲线叠加 + 图例 | ✅ | 颜色分配+图例优化 |
| 8 | **P1** | S2 | probes_only 结果自动解析 | ✅ | SimRunner 序列化+自动加载 |
| 9 | **P2** | S6 | 仿真统计面板（timing/memory） | ✅ | StatisticsPanel 已创建 |
| 10 | **P2** | S6 | CSV 导出增强 | ✅ | 批量+line_current+缓存 |
| 11 | **P2** | S6 | Python 脚本导出 | ✅ | PlotPanel 脚本导出按钮 |
| 12 | **P2** | UX | 撤销栈 UX 改进 | ✅ | 工具栏按钮+状态同步 |
| 13 | **P3** | UX | 信号命名层（双击连线改名） | ❌ | 可读性增强 |
| 14 | **P3** | S7 | 10 个示例项目 | ❌ | 覆盖主要功能 |
| 15 | **P3** | S7 | 错误提示优化 | ❌ | 用户友好性 |

---

## 二、按阶段展开详情

### S0 内核对齐 — 剩余 5%

| 任务ID | 任务 | 具体工作 | 预估 |
|--------|------|---------|------|
| S0.11 | 验证清单 | 用 5 个现有 `case*.emtp` 文件通过 GUI → 代码生成 → emtp_0508 运行，确认输出正确 | 1天 |

**5 个案例文件**：`case_rc.emtp`, `case_rlc.emtp`, `case_switching.emtp`, `case_lightning_cable.emtp`, `case_example.emtp`

---

### S1 仿真运行器增强 — 基础版已完成，增强项待做

| 任务ID | 任务 | 具体工作 | 当前状态 | 预估 |
|--------|------|---------|---------|------|
| S1.1 | QProcess 替代 | 将 `QThread + subprocess` 改为 `QProcess`，实现可靠 stdout 流式读取 | ❌ 当前用 QThread | 2天 |
| S1.2 | 进度解析 | 从 emtp_0508 stdout 提取 step/total，发出 `progress_updated` 信号 | ❌ | 1天 |
| S1.3 | 结果序列化 | probes_only 模式 JSON 输出解析 → dict 结构供 PlotPanel 使用 | ❌ | 1天 |
| S1.4 | 运行目录管理 | 每次运行创建独立临时目录，仿真结束归档 | ❌ | 0.5天 |
| S1.5 | MainWindow 集成 | 进度条 `QProgressBar` + 日志文本框 `QPlainTextEdit` + 状态标签 | ❌ | 1天 |

**已完成项**：
- ✅ `log_received = Signal(str)` 信号
- ✅ 运行中禁用/恢复运行按钮
- ✅ 取消功能

---

### S2 结果查看器增强 — 基础版已完成，核心增强待做

| 任务ID | 任务 | 具体工作 | 当前状态 | 预估 |
|--------|------|---------|---------|------|
| S2.1 | 光标测量 | matplotlib 十字线 + 鼠标悬停数值显示 + 双光标 Δt/ΔV | ❌ | 2天 |
| S2.2 | 多曲线叠加 | 多探针同时绘图 + 图例 + 颜色分配 | ❌ | 1天 |
| S2.3 | probes_only 结果自动解析 | 仿真完成后自动解析 JSON 结果并填充波形 | ❌ | 1天 |
| S2.4 | 缩放与平移增强 | 鼠标滚轮缩放 + 拖拽平移 + 重置视图 | 🔶 部分可用 | 0.5天 |

**已完成项**：
- ✅ 时间单位切换 (s/ms/μs/ns)
- ✅ 探针选择下拉框 (全部/单个)
- ✅ line_current 探针支持（点线样式）
- ✅ 数据缓存 + `_replot()` 快速重绘
- ✅ CSV/PNG 导出（基础版）

---

### S3 验证引擎 — 引擎完成，UI 集成待做

| 任务ID | 任务 | 具体工作 | 当前状态 | 预估 |
|--------|------|---------|---------|------|
| S3.1 | ValidationPanel | 新建 `ui/validation_panel.py`，持久化错误列表面板（级别图标+定位跳转） | ❌ | 1天 |
| S3.2 | MainWindow 集成 | 工具栏 Validate 按钮 + F4 快捷键 + 面板停靠 | 🔶 F4已有,面板未做 | 0.5天 |
| S3.3 | 错误定位跳转 | 点击验证错误项 → 画布高亮对应元件/连线 | ❌ | 1天 |

**已完成项**：
- ✅ `core/validator.py` — 8种校验规则全部实现

---

### S6 统计与导出 — 全部未开始

| 任务ID | 任务 | 具体工作 | 预估 |
|--------|------|---------|------|
| S6.1 | StatisticsPanel | 新建 `ui/statistics_panel.py`，显示 timing（矩阵构建/LU分解/求解/MOA段切换/LPM重解）| 1天 |
| S6.2 | CSV 批量导出 | 导出所有探针数据为 CSV（含表头+时间列+编码BOM） | 0.5天 |
| S6.3 | Python 脚本导出 | 导出独立可运行的 .py 脚本（含 import + 完整配置） | 0.5天 |

---

### S7 打磨与示例 — 全部未开始

| 任务ID | 任务 | 具体工作 | 预估 |
|--------|------|---------|------|
| S7.1 | 示例项目（10个） | RLC充放电/雷击ULM/LCP架空线/LCP电缆/MOA保护/LPM闪络/级联电缆/多相验证/UMEC变压器/综合案例 | 3天 |
| S7.2 | 错误提示优化 | 用户友好的错误对话框 + 操作提示 + tooltip | 1天 |
| S7.3 | 文档 | README + 快速入门 + 元件参数说明 | 1天 |

---

### UX 改进（跨阶段）

| 任务ID | 任务 | 具体工作 | 预估 |
|--------|------|---------|------|
| UX.1 | 撤销栈 UX | Undo/Redo 工具栏按钮 + 快捷键提示 (Ctrl+Z/Ctrl+Y) | 0.5天 |
| UX.2 | 信号命名 | 双击连线 → 弹出重命名对话框，提高电路可读性 | 1天 |

---

## 三、开发计划文档中的不一致项

> 以下为 `emtp_gui_dev_plan_v2.md` v2.3 中与实际代码状态不一致的地方，需要更新：

| 位置 | 问题 | 修正 |
|------|------|------|
| §3.3 S5.2 MOA 避雷器表单 | 标记为 ❌ | → ✅ 已完成 |
| §3.3 S5.3 LPM 绝缘子表单 | 标记为 ❌ | → ✅ 已完成 |
| §四 S0.9 探针系统适配 | 列为"待完成" | → ✅ 已完成 |
| §四 S0.10 result_mode 适配 | 列为"待完成" | → ✅ 已完成 |
| §四 ⚠️ UMEC numpy 缺失 BUG | 列为待修复 | → ✅ 已修复 (np. → math.) |
| §五 目录结构 `moa_param_dialog.py` | 出现两次（一行✅一行❌→✅） | 删除重复行 |
| §五 目录结构 `lpm_param_dialog.py` | 出现两次（一行✅一行❌→✅） | 删除重复行 |
| §六 风险表 UMEC numpy 缺失 | 标记为"✅ 已修复"但原文有删除线格式问题 | 统一格式 |

---

## 四、建议执行顺序

```
第一批（核心闭环，~5天）
├── S0.11  验证清单（5案例端到端）          ← 最高优先，验收门
├── S3.1   ValidationPanel UI              ← 已有引擎，补 UI
├── S2.3   probes_only 结果自动解析         ← 仿真→波形闭环
└── 修复开发计划文档不一致项

第二批（体验提升，~5天）
├── S1.1   QProcess 替代                   ← 稳定性
├── S1.2   进度解析 + 进度条               ← 用户体验
├── S2.1   光标测量                        ← 分析能力
└── S2.2   多曲线叠加                      ← 对比分析

第三批（功能补全，~4天）
├── S1.3   结果序列化
├── S1.4   运行目录管理
├── S1.5   MainWindow 仿真面板集成
├── S3.2   MainWindow 验证面板集成
└── S3.3   错误定位跳转

第四批（收尾打磨，~5天）
├── S6.1   统计面板
├── S6.2   CSV 批量导出
├── S6.3   Python 脚本导出
├── UX.1   撤销栈 UX
└── UX.2   信号命名

第五批（发布准备，~5天）
├── S7.1   10个示例项目
├── S7.2   错误提示优化
└── S7.3   文档
```

**总预估**：~24 天（单人兼职）

---

## 五、已完成模块一览（不再重复开发）

| 阶段 | 已完成项 |
|------|---------|
| S0 | 雷电源6模型 / LCP电缆引脚+代码生成 / 子电路展平 / 向后兼容 / LCP参数对话框(3种) / 探针系统(3种) / result_mode / UMEC三相组 / numpy BUG修复 |
| S1 | SimRunner 基础版 + log_received 信号 + 运行中禁用按钮 + 取消功能 |
| S2 | PlotPanel 基础版 + 时间单位切换 + 探针选择 + line_current + 数据缓存 |
| S3 | Validator 引擎 8种校验规则 + F4快捷键 |
| S4 | LCP 架空线/单芯/三芯参数对话框 + 截面预览 |
| S5 | UMEC/MOA/LPM 参数对话框 + PropertyPanel 配置按钮 |
